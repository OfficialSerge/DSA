#ifndef BINARY_SEARCH_H
#define BINARY_SEARCH_H

#include <vector>

/**
 * Search for the needle in the haystack using binary search.
 */
bool binary_search(const std::vector<int> &haystack, int needle);

#endif // !BINARY_SEARCH_H
