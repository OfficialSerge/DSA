#ifndef TWO_CRYSTAL_BALLS_H
#define TWO_CRYSTAL_BALLS_H

#include <vector>

/**
 * Given 2 identical crystal balls, return the first floor the ball breaks on or -1.
 * 
 * @param breaks boolean vector, true if the ball breaks, false otherwise.
 * @return first floor where the ball breaks or -1.
 */
int two_crystal_balls(const std::vector<bool> &breaks);

#endif // !TWO_CRYSTAL_BALLS_H
